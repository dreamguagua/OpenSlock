# 复制为 .env 并按需修改。

# 应用 / 测试运行时:用非超级用户 crew_app 连接 (RLS 才会生效!)
DATABASE_URL=postgres://crew_app:crew_app_pw@127.0.0.1:5432/crew_dev

# 一次性建库/迁移/建角色/建策略时:用 owner 或超级用户连接
# (放在 shell 里临时 export,不必长期留在 .env)
# ADMIN_URL=postgres://youruser@127.0.0.1:5432/crew_dev
